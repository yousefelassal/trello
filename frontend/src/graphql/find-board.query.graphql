query findBoard($id: ID!) {
    findBoard(id: $id){
        id
        bg
        title
        description
        lists{
            id
            title
            position
            cards{
                id
                title
                description
                position
            }
        }
    }
}