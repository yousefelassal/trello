query findBoard($id: ID!) {
    findBoard(id: $id){
        id
        bg
        title
        description
        lists{
            id
            title
            cards{
                id
                title
                description
            }
        }
    }
}